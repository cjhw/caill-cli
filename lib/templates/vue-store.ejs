import { defineStore } from 'pinia';
import { ref } from 'vue';

export const <%= data.name %>Store = defineStore('<%= data.lowerName %>', {
  state: () => ({
  }),
  getters: {
  },
  actions: {
  },
});
